import 'package:flutter_test/flutter_test.dart';
import 'package:kynos/app/shell_page.dart';

void main() {
  test('ShellPage type is available for animated tab shell', () {
    expect(ShellPage, isA<Type>());
  });
}
