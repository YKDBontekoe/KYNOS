# Copy this file to .env for local development overrides (optional).
# .env is gitignored — never commit secrets.
#
# HuggingFace access tokens are configured in the app under
# Settings → AI & Cloud → HuggingFace access token.
